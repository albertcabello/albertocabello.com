server {
	listen 80 default_server;
	listen [::]:80 default_server ipv6only=on;

	server_name test.albertocabello.com www.test.albertocabello.com;
	root /var/www/ssl-setup;

	location ~ /.well-known/acme-challenge/ {
		allow all;
		root /var/www/ssl-setup;
	}

	index index.html;
}
